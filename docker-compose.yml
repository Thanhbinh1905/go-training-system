
services:
  # ================= DATABASES =================
  user-db:
    image: postgres:13.21-alpine3.21
    container_name: user-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: user
    ports:
      - "5432:5432"
    networks:
      - backend-net
    volumes:
      - user-data:/var/lib/postgresql/data
  team-db:
    image: postgres:13.21-alpine3.21
    container_name: team-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: team
    ports:
      - "5433:5432"
    networks:
      - backend-net
    volumes:
      - team-data:/var/lib/postgresql/data
  asset-db:
    image: postgres:13.21-alpine3.21
    container_name: asset-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: asset
    ports:
      - "5434:5432"
    networks:
      - backend-net
    volumes:
      - asset-data:/var/lib/postgresql/data
  # ================= SERVICES =================
  migrate:
    build:
      context: .
      dockerfile: migration/Dockerfile
    container_name: migrate-service
    depends_on:
      - user-db
      - team-db
      - asset-db
    environment:
      USER_DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@user-db:5432/user?sslmode=disable
      TEAM_DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@team-db:5432/team?sslmode=disable
      ASSET_DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@asset-db:5432/asset?sslmode=disable
    restart: "no"
    networks:
      - backend-net
  user-service:
    build:
      context: .
      dockerfile: services/user-service/Dockerfile
    container_name: user-service
    ports:
      - "8081:8080"
    depends_on:
      - user-db
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@user-db:5432/user?sslmode=disable
      JWT_SECRET: ${JWT_SECRET}
      PRODUCTION: ${PRODUCTION}
      GRPC_PORT: 50051
    networks:
      - backend-net
      
  team-service:
    build:
      context: .
      dockerfile: services/team-service/Dockerfile
    container_name: team-service
    ports:
      - "8082:8080"
    depends_on:
      - team-db
      - user-service
    volumes:
      - ./services/team-service/logs:/app/logs
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@team-db:5432/team?sslmode=disable
      PRODUCTION: ${PRODUCTION}
    networks:
      - backend-net
#   # ================= MONITORING =================
#   loki:
#     image: grafana/loki:2.9.4
#     container_name: loki
#     ports:
#       - "3100:3100"
#     command: -config.file=/etc/loki/local-config.yaml
#     networks:
#       - monitoring-net

#   promtail:
#     image: grafana/promtail:2.9.4
#     container_name: promtail
#     volumes:
#       - ./services/team-service/logs:/var/log/team-service
#       - ./promtail-config.yml:/etc/promtail/promtail-config.yml
#     command: -config.file=/etc/promtail/promtail-config.yml
#     depends_on:
#       - loki
#     networks:
#       - monitoring-net
#       - backend-net 

#   prometheus:
#     image: prom/prometheus
#     container_name: prometheus
#     ports:
#       - "9090:9090"
#     volumes:
#       - ./prometheus.yml:/etc/prometheus/prometheus.yml
#     command:
#       - '--config.file=/etc/prometheus/prometheus.yml'
#       - '--web.enable-lifecycle'
#     networks:
#       - monitoring-net
#       - backend-net 

#   grafana:
#     image: grafana/grafana:10.3.1
#     container_name: grafana
#     ports:
#       - "3000:3000"
#     environment:
#       GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USERNAME}
#       GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
#     depends_on:
#       - loki
#       - prometheus
#     networks:
#       - monitoring-net

# # ================= VOLUMES & NETWORKS =================

volumes:
  user-data:
  team-data:
  asset-data:

networks:
  backend-net:
    driver: bridge
  monitoring-net:
    driver: bridge
